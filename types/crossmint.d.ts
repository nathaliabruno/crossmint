export type Goal = {
  goal: Array;
} & Record<array, unknown>;

export type Polyanets = {
  polyanets: Array;
} & Record<array, unknown>;

export type Comeths = {
  comeths: Array;
} & Record<array, unknown>;

export type Soloons = {
  soloons: Array;
} & Record<array, unknown>;
